<div id="group_details_modal_content" class="group-details-content">
    <!-- MAIN VIEW -->
    <div id="group-details-view" class="details-wrapper">
        <!-- HEADER -->
        <div class="group-header">
            <img src="/images/avatar/default-avatar.jpg" alt="Group Avatar" class="group-avatar" id="groupAvatar">
            <h3 class="group-name" id="groupName">Unnamed Group</h3>
        </div>

        <!-- MEMBER LIST -->
        <div class="member-list" id="memberList">
            <!-- ✅ Nội dung sẽ được load bởi JavaScript -->
        </div>

        <!-- ACTIONS BUTTONS -->
        <div class="group-actions">
            <button class="add-member-btn">Add Member</button>
            <button class="leave-group-btn">Leave Group</button>
        </div>
    </div>

    <!-- CONFIRMATION VIEW -->
    <div id="remove-member-confirmation-view" style="display: none;"></div>
</div>

<!-- ========================================
     POPUP WRAPPERS
     ======================================== -->
<div id="addMemberPopup" style="display: none;">
    <div class="add-member-content">
        <h4 class="add-member-title">Add Member</h4>
        <div class="member-selection" id="memberSelection"></div>
        <div class="member-actions">
            <button class="ok-btn">OK</button>
            <button class="cancel-btn">Cancel</button>
        </div>
    </div>
</div>

<div id="leaveConfirmationPopup" style="display: none;">
    <div class="confirmation-content">
        <h4 class="confirmation-title">Confirm Leave Group</h4>
        <p>Are you sure you want to leave this group?</p>
        <div class="confirmation-actions">
            <button class="confirm-leave">Yes</button>
            <button class="cancel-leave">No</button>
        </div>
    </div>
</div>

<div id="removeMemberPopup" style="display: none;">
    <div class="confirmation-content">
        <h4 class="confirmation-title">Remove Member</h4>
        <p id="removeMemberMessage">Are you sure?</p>
        <div class="confirmation-actions">
            <button class="cancel-remove">No</button>
            <button class="confirm-remove">Yes</button>
        </div>
    </div>
</div>

<style>
    /* ========================================
           🎨 MODAL OVERRIDE
           ======================================== */
    #group_details_modal.modal.show ~ .modal-backdrop {
        display: none !important;
    }

    #group_details_modal {
        background: transparent !important;
    }

        #group_details_modal .modal-dialog {
            max-width: 680px;
            margin: 1rem auto;
        }

        #group_details_modal .modal-header {
            background: linear-gradient(135deg, #2c3e50, #34495e) !important;
            border-bottom: 1px solid #3a3a3a !important;
            padding: 10px 20px !important;
        }

        #group_details_modal .modal-title {
            color: #00e1ff !important;
            font-weight: 600 !important;
            font-size: 1.1em;
        }

        #group_details_modal .btn-close {
            filter: invert(1) brightness(1.5) !important;
            opacity: 0.8 !important;
        }

            #group_details_modal .btn-close:hover {
                opacity: 1 !important;
                transform: scale(1.1);
            }

        #group_details_modal .modal-body {
            padding: 0 !important;
            background: transparent !important;
        }

    /* ========================================
           📦 MAIN CONTAINER
           ======================================== */
    .group-details-content {
        background: linear-gradient(135deg, #1e1e1e, #2d2d2d);
        border-radius: 12px;
        width: 100%;
        max-height: calc(100vh - 140px);
        color: #fff;
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.6);
        display: flex;
        flex-direction: column;
    }

    .details-wrapper {
        display: flex;
        flex-direction: column;
        max-height: calc(100vh - 140px);
        overflow: hidden;
    }

    /* ========================================
           📌 HEADER
           ======================================== */
    .group-header {
        flex-shrink: 0;
        text-align: center;
        padding: 12px;
        border-bottom: 1px solid #3a3a3a;
        background: linear-gradient(135deg, #2c3e50, #34495e);
    }

    .group-avatar {
        width: 65px;
        height: 65px;
        object-fit: cover;
        border: 3px solid #00e1ff;
        border-radius: 50%;
        cursor: pointer;
        transition: transform 0.3s;
        margin-bottom: 6px;
    }

        .group-avatar:hover {
            transform: scale(1.08);
            box-shadow: 0 4px 15px rgba(0, 225, 255, 0.4);
        }

    .group-name {
        font-size: 1.2em;
        color: #00e1ff;
        margin: 0;
        font-weight: 600;
    }

    /* ========================================
           📜 MEMBER LIST - SCROLLABLE
           ======================================== */
    .member-list {
        flex: 1 1 auto;
        overflow-y: auto;
        overflow-x: hidden;
        padding: 10px;
        background: #1a1a1a;
        min-height: 150px;
    }

        .member-list::-webkit-scrollbar {
            width: 6px;
        }

        .member-list::-webkit-scrollbar-track {
            background: #0d0d0d;
            border-radius: 3px;
        }

        .member-list::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, #3a3a3a, #2a2a2a);
            border-radius: 3px;
        }

            .member-list::-webkit-scrollbar-thumb:hover {
                background: linear-gradient(180deg, #4a4a4a, #3a3a3a);
            }

    .member-item {
        display: flex;
        align-items: center;
        padding: 8px;
        border-bottom: 1px solid #2a2a2a;
        border-radius: 6px;
        margin-bottom: 5px;
        background: rgba(52, 73, 94, 0.3);
        transition: all 0.2s;
    }

        .member-item:hover {
            background: rgba(52, 73, 94, 0.6);
            transform: translateX(5px);
        }

    .member-avatar {
        width: 36px;
        height: 36px;
        margin-right: 8px;
        border-radius: 50%;
        object-fit: cover;
        border: 2px solid #00e1ff;
    }

    .member-name {
        flex-grow: 1;
        font-size: 0.9em;
        color: #e0e0e0;
        font-weight: 500;
    }

    .remove-member-icon {
        width: 28px;
        height: 28px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 50%;
        background: rgba(255, 77, 79, 0.2);
        color: #ff6b6b;
        font-weight: 700;
        font-size: 1.1em;
        border: 2px solid rgba(255, 77, 79, 0.4);
        cursor: pointer;
        transition: all 0.2s;
    }

        .remove-member-icon:hover {
            background: #ff4d4f;
            color: #fff;
            transform: scale(1.15) rotate(90deg);
            box-shadow: 0 3px 10px rgba(255, 77, 79, 0.5);
        }

    /* ========================================
           📌 ACTIONS - ALWAYS VISIBLE
           ======================================== */
    .group-actions {
        flex-shrink: 0;
        display: flex !important;
        justify-content: space-between;
        gap: 10px;
        padding: 10px 18px;
        border-top: 2px solid #3a3a3a;
        background: linear-gradient(135deg, #2c3e50, #34495e);
        height: 55px;
    }

    .add-member-btn,
    .leave-group-btn {
        flex: 1;
        padding: 9px;
        border-radius: 8px;
        font-weight: 600;
        font-size: 0.85em;
        border: none;
        cursor: pointer;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        transition: all 0.2s;
    }

    .add-member-btn {
        background: linear-gradient(135deg, #00bcd4, #0097a7);
        color: #fff;
    }

        .add-member-btn:hover {
            background: linear-gradient(135deg, #0097a7, #00838f);
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(0, 188, 212, 0.5);
        }

    .leave-group-btn {
        background: linear-gradient(135deg, #e74c3c, #c0392b);
        color: #fff;
    }

        .leave-group-btn:hover {
            background: linear-gradient(135deg, #c0392b, #a93226);
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(231, 76, 60, 0.5);
        }

    /* ========================================
           🔔 POPUP OVERLAYS - FIXED POSITIONING
           ======================================== */

    /* ✅ KEY FIX: Popup overlays */
    #addMemberPopup,
    #leaveConfirmationPopup,
    #removeMemberPopup {
        display: none; /* ✅ HIDDEN BY DEFAULT */
        position: fixed !important;
        top: 0 !important;
        left: 0 !important;
        width: 100vw !important;
        height: 100vh !important;
        background: rgba(0, 0, 0, 0.85) !important;
        z-index: 999999 !important;
        align-items: center !important;
        justify-content: center !important;
        margin: 0 !important;
        padding: 0 !important;
    }

        /* ✅ Show popup when active */
        #addMemberPopup[style*="display: flex"],
        #addMemberPopup[style*="display: block"],
        #leaveConfirmationPopup[style*="display: flex"],
        #leaveConfirmationPopup[style*="display: block"],
        #removeMemberPopup[style*="display: flex"],
        #removeMemberPopup[style*="display: block"] {
            display: flex !important;
        }

    .add-member-content,
    .confirmation-content {
        position: relative;
        background: linear-gradient(135deg, #1e1e1e, #2d2d2d);
        padding: 25px;
        border-radius: 12px;
        max-width: 550px;
        width: 90%;
        color: #fff;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.9);
        margin: auto;
    }

    .add-member-title,
    .confirmation-title {
        font-size: 1.4em;
        margin-bottom: 18px;
        text-align: center;
        font-weight: 600;
    }

    .add-member-title {
        color: #00bcd4 !important;
    }

    .confirmation-title {
        color: #ff4444 !important;
    }

    .member-selection {
        background: #252525;
        border-radius: 10px;
        padding: 10px;
        max-height: 300px;
        overflow-y: auto;
        margin-bottom: 15px;
    }

        .member-selection::-webkit-scrollbar {
            width: 6px;
        }

        .member-selection::-webkit-scrollbar-track {
            background: #1a1a1a;
            border-radius: 3px;
        }

        .member-selection::-webkit-scrollbar-thumb {
            background: #3a3a3a;
            border-radius: 3px;
        }

            .member-selection::-webkit-scrollbar-thumb:hover {
                background: #4a4a4a;
            }

    .member-option {
        display: flex;
        align-items: center;
        padding: 8px;
        border-bottom: 1px solid #3a3a3a;
        border-radius: 6px;
        margin-bottom: 5px;
        cursor: pointer;
        transition: background 0.2s;
    }

        .member-option:last-child {
            border-bottom: none;
            margin-bottom: 0;
        }

        .member-option:hover {
            background: #2e2e2e;
        }

    .member-actions,
    .confirmation-actions {
        display: flex;
        justify-content: flex-end;
        gap: 10px;
        margin-top: 15px;
    }

    .confirmation-actions {
        justify-content: center;
    }

    .ok-btn,
    .cancel-btn,
    .confirm-leave,
    .cancel-leave,
    .confirm-remove,
    .cancel-remove {
        padding: 10px 20px;
        border-radius: 8px;
        font-weight: 600;
        font-size: 0.9em;
        border: none;
        cursor: pointer;
        transition: all 0.2s;
    }

    .ok-btn {
        background: #28a745;
        color: #fff;
    }

        .ok-btn:hover {
            background: #218838;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(40, 167, 69, 0.4);
        }

    .cancel-btn,
    .cancel-leave,
    .cancel-remove {
        background: #6c757d;
        color: #fff;
    }

        .cancel-btn:hover,
        .cancel-leave:hover,
        .cancel-remove:hover {
            background: #5a6268;
            transform: translateY(-2px);
        }

    .confirm-leave,
    .confirm-remove {
        background: #dc3545;
        color: #fff;
    }

        .confirm-leave:hover,
        .confirm-remove:hover {
            background: #c82333;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(220, 53, 69, 0.4);
        }

    /* ========================================
           📱 RESPONSIVE
           ======================================== */
    @@media (max-width: 768px) {
        .details-wrapper

    {
        max-height: calc(100vh - 100px);
    }

    .group-actions {
        flex-direction: column;
        height: auto;
        padding: 10px 15px;
    }

    .add-member-content,
    .confirmation-content {
        width: 95%;
        padding: 20px;
    }

    .confirmation-actions,
    .member-actions {
        flex-direction: column;
    }

    .ok-btn,
    .cancel-btn,
    .confirm-leave,
    .cancel-leave,
    .confirm-remove,
    .cancel-remove {
        width: 100%;
    }

    }
</style>